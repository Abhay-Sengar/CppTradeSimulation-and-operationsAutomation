---
# Default inventory: run the playbook directly on the trading node itself, with
# no SSH hop. This is how the stack is normally provisioned:
#   ansible-playbook -i inventory/local.yml playbooks/site.yml -K
all:
  children:
    trading_nodes:
      hosts:
        trading-node:
          ansible_connection: local
